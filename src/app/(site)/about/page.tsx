import AboutPageClient from "@/components/AboutPageClient";

export const metadata = {
  title: "About — JANES",
  description: "Our story, values, and the vision behind JANES fashion.",
};

export default function AboutPage() {
  return <AboutPageClient />;
}
